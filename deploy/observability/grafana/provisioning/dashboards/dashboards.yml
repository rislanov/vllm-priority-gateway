apiVersion: 1

providers:
  - name: LLM Gateway
    orgId: 1
    folder: LLM Gateway
    type: file
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 5
    options:
      path: /var/lib/grafana/dashboards
